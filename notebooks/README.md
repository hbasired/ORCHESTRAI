# Stage 4/5 — training notebooks (Colab)

## ▶ TRAIN THESE TWO NOW (2026-06-01) — run both, send back both zips
1. **`stage04b_xgboost_pdm_colab.ipynb`** → `pdm_xgb_brain.zip` — a **stronger** PdM brain (XGBoost vs the MLP)
   + a **recall-tuned threshold** (catch more failures). Addresses G-034 + G-033. No GPU needed.
2. **`stage05_demand_forecasting_colab.ipynb`** → `demand_brain.zip` — Stage 5 LSTM demand forecaster
   (UCI Bike Sharing, **chronological split = leakage-free**, beats persistence/seasonal-naive baselines). Use GPU.

Run all in each, download both zips, send them back. When the XGBoost brain beats the MLP I'll wire it as the
Stage-4 brain; the demand brain wires into Stage 5. (The already-accepted Stage-4 MLP brain is live in `models/`.)

---

## ⚠️ USE THIS ONE → `stage04_ai4i_tabular_colab.ipynb` (corrected, 2026-06-01)

The first two attempts were **rejected on review** — do not use their brains:
- `synthetic/` — **degenerate**: the test split had 0 failure windows → `roc_auc: NaN`, `pr_auc: 0.0` (generator-labeling bug).
- `real/` (the AI4I one edited in Gemini) — its 0.99 AUC is **data leakage**: it random-split overlapping sliding
  windows (the notebook's own comment admits it) and treated all 10k independent tabular rows as one machine. Not trustworthy.

**Run `stage04_ai4i_tabular_colab.ipynb` instead** — AI4I 2020 as a proper tabular classifier: no windowing,
stratified split (no leakage), leaky `TWF/HDF/PWF/OSF/RNF` columns dropped, honest metrics. Same `pdm_brain.zip`
output — send it back. **Honest expectation:** ROC-AUC ~0.93–0.98, a lower PR-AUC (data is ~3.4% failures —
that gap is correct). The notebook ends with a sanity checklist so we can trust the number.

---

## (deprecated) original time-series notebook

`stage04_predictive_maintenance_colab.ipynb` trains the **PREDICT** step of the self-healing engine
(KB_25 step 1): a BiLSTM + attention model that forecasts **P(failure within H steps)** from a window of
machine telemetry.

## Run it (free GPU)
1. Open the notebook in **Google Colab**.
2. `Runtime ▸ Change runtime type ▸ GPU (T4)`.
3. `Runtime ▸ Run all`.
4. The last cell downloads **`pdm_brain.zip`** — **send that file back.** (No API key needed; training is pure local ML.)

## What's inside `pdm_brain.zip`
| File | What it is |
|---|---|
| `pdm_failure_predictor.pt` | torch `state_dict` (the weights — the "brain") |
| `scaler.pkl` | the feature `StandardScaler` (needed for inference) |
| `model_meta.json` | architecture + window/horizon/threshold + feature names |
| `metrics.json` | ROC-AUC, PR-AUC, mean lead-time vs the all-healthy baseline |

## Dataset
Default = **self-contained synthetic degradation** (realistic bearing-wear physics, license-clean, no download).
An optional **AI4I 2020** (UCI, CC BY 4.0) cell is included if you prefer a real open-source dataset — record
its license in `knowledge-base/KB_03_Datasets_Catalog.md` if you use it.

## After you send the brain back
I'll wire it into `backend/ml/failure_predictor.py` (`predict_failure(window) -> {target_id, p_fail, horizon,
confidence}`), drop the weights under `models/`, fill `compliance/model-cards/pdm_failure_predictor.md` from
`metrics.json`, add the eval to KB_23, and close Stage 4 (audit must drop below 411 by removing the stub predictor).

The notebook is regenerated by `python scripts/build_stage04_notebook.py` (keep edits in that generator).
