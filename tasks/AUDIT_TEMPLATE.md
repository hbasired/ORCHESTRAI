# Per-task audit report (template)

> Auto-generated by `scripts/audit-task.sh`. This template is the canonical shape; the live versions live in `audits/STAGE_<NN>_audit.md`.

**Date**: YYYY-MM-DDTHH:MM:SSZ
**Stage**: NN (or NN_5 for half-stages)
**Task doc**: tasks/STAGE_NN_<slug>.md
**Baseline (pre-stage)**: <int from `.audit-baseline` at stage open>
**Audit total (current)**: <int>

## 1. Audit script

```
<captured output of `bash scripts/audit.sh`>
```

Gate: total ≤ baseline. Strictly < unless stage was flagged `--no-baseline-drop`.

## 2. KB diff coverage

Working-tree modified KB files (vs the task doc's "KB files this stage updates" block):

| KB file | In task doc? | Modified? | Substantive diff? |
|---|---|---|---|
| KB_NN_x | yes | yes/no | yes/no |

## 3. Missing artefacts

### Model cards / metrics

- (list `models/<x>.metrics.json` or `compliance/model-cards/<x>.md` missing for any weight file present)

### New ADRs

- (list untracked `compliance/decision-logs/*.md` files, or note "(none)" if no architectural decision was made)

## 4. Vulnerabilities

### Gitleaks

```
<gitleaks output>
```

### Classical-crypto sniffer (Stage 13.5+)

- (list `backend/crypto/` files containing fresh `rsa.generate_private_key`, `RSAPrivateKey`, `ec.generate_private_key`, `ECDSA`, `EllipticCurvePrivateKey`)

### Actuator paths without safety.validator reference (Stage 17+)

- (list files matching `actuator|send_command|execute_action` without a `safety.validator` reference)

## 5. Audit chain integrity

`scripts/verify-audit-chain.py --quick` status: OK / empty / not-yet-enabled / FAIL

## 6. Gaps (action items)

(Each unchecked item refuses `scripts/close-task.sh`. Run `scripts/rectify-task.sh <stage>` to loop back into implementation.)

- [ ]
- [ ]

## 7. Sign-off

- Audit ran by: <role persona>
- Result: clean / has-gaps
- Next: `scripts/rectify-task.sh <stage>` or `scripts/close-task.sh <stage>`
